.DS_Store
.claude/worktrees/

# Local dev only — never commit
admin/config.local.php

# Generated / live-edited on server — don't overwrite on pull
data/backups/
creations/blogs/backups/
# data/albums.json is live-edited on server — run after deploy:
# git update-index --assume-unchanged data/albums.json

# Binary assets — managed on server directly, not via git
images/

# Delete from server after running once
admin/setup.php
